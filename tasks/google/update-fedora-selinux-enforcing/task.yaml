summary: Update fedora image with the latest updates

systems: [fedora-*]

environment:
    IMAGE: fedora-27-64-selinux-enforcing-v$(date +'%Y%m%d')
    FAMILY: fedora-27-64-selinux-enforcing
    DESCRIPTION: Fedora 27 64 bits with SELinux enforcing
    DISK: $(hostname)

execute: |
    . "$TESTSLIB/google.sh"
    . "$TESTSLIB/utils.sh"

    # Make the upgrade
    dnf makecache
    dnf upgrade -y
    dnf clean all -y

    sed -i 's/SELINUX=.*/SELINUX=enforcing/g' /etc/selinux/config

    # Clean disk before create the shapshot
    clean_machine

    # Create a new image based on the current disk
    create_image_from_disk $IMAGE $FAMILY $DESCRIPTION $DISK

restore: |
    . "$TESTSLIB/google.sh"

    delete_snapshot "$DISK"
